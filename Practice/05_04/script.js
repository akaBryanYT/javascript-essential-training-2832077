/**
 * Note: This file is intentionally empty.
 * You can use it to test your skills at traversing the DOM using JavaScript.
 */

const element1 = document.querySelector("body");
const element2 = document.querySelectorAll(".backpack");
const element3 = document.querySelectorAll(".feature");
const backpackVolumeList = document.querySelectorAll(".feature.backpack__volume");

console.log(element1);
console.log(element2);
console.log(element3);
console.log(backpackVolumeList);
